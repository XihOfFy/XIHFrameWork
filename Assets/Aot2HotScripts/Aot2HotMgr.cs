using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using TMPro;
namespace Aot2Hot
{
    public class Aot2HotMgr : MonoBehaviour
    {
        public TMP_Text tip;
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("Hello,热更场景");
            tip.text = "aaaaaaaaaaaaaaaaaaaaaaa";
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
