using FairyGUI.Utils;
using FairyGUI;
using XiHUI;
using Tmpl;
using XiHUtil;
using XiHSound;

namespace Hot
{
    /*
    [UIPackageItemExtension("ui://Common/GoldBtn")]
    public class GoldAddBtn : CoinBtn
    {
        public override void ConstructFromXML(XML xml)
        {
            base.ConstructFromXML(xml);
            this.onClick.Set(OnClick);
        }
        async void OnClick()
        {
            SoundMgr.Instance.PlayBtnSound();
            var dlg = await UIUtil.OpenDialogAsync<CoinBuyDialog>();//打开购买弹框
            dlg.Show(iconImg);
        }
    }*/
}
